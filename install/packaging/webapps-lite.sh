# Omarchy Lite - No opinionated web apps installed by default
# Users can add their own via omarchy-webapp-install

# Keep only essential/utility web apps (none for now)
# Users can manually install: omarchy-webapp-install "AppName" "https://url" "icon.png"
