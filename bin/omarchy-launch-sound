#!/bin/bash

if [[ "$TERMINAL" =~ ^(ghostty)$ ]]; then
    exec setsid uwsm app -- "$TERMINAL" --class=com.omarchy.wiremix -e wiremix "$@"
else
    exec setsid uwsm app -- "$TERMINAL" --class=Wiremix -e wiremix "$@"
fi
