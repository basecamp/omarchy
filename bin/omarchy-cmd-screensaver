#!/bin/bash

# Run the Omarchy screensaver using random effects from TTE.

ensure_tte() {
  if tte --version >/dev/null 2>&1; then
    return 0
  fi

  local tte_site=""
  for p in /usr/lib/python*/site-packages/terminaltexteffects; do
    if [[ -d "$p" ]]; then
      tte_site="${p%/terminaltexteffects}"
      break
    fi
  done

  if [[ -n "$tte_site" ]]; then
    export PYTHONPATH="$tte_site${PYTHONPATH:+:$PYTHONPATH}"
  fi

  if ! tte --version >/dev/null 2>&1; then
    notify-send "Screensaver error" "tte failed to start. Try reinstalling python-terminaltexteffects."
    echo "tte failed to start. Reinstall python-terminaltexteffects." >&2
    exit 1
  fi
}

screensaver_in_focus() {
  hyprctl activewindow -j | jq -e '.class == "org.omarchy.screensaver"' >/dev/null 2>&1
}

exit_screensaver() {
  hyprctl keyword cursor:invisible false &>/dev/null || true
  pkill -x tte 2>/dev/null
  pkill -f org.omarchy.screensaver 2>/dev/null
  exit 0
}

# Exit the screensaver on signals and input from keyboard and mouse
trap exit_screensaver SIGINT SIGTERM SIGHUP SIGQUIT

printf '\033]11;rgb:00/00/00\007'  # Set background color to black

hyprctl keyword cursor:invisible true &>/dev/null

tty=$(tty 2>/dev/null)

ensure_tte

while true; do
  tte -i ~/.config/omarchy/branding/screensaver.txt \
    --frame-rate 120 --canvas-width 0 --canvas-height 0 --reuse-canvas --anchor-canvas c --anchor-text c\
    --random-effect --exclude-effects dev_worm \
    --no-eol --no-restore-cursor &

  while pgrep -t "${tty#/dev/}" -x tte >/dev/null; do
    if read -n1 -t 1 || ! screensaver_in_focus; then
      exit_screensaver
    fi
  done
done
