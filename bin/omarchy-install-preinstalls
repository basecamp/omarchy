#!/bin/bash

# omarchy:summary=Restore selected preinstalled Omarchy applications (web apps, TUIs, and selected packages).
# omarchy:args=[--all|name...]
# omarchy:examples=omarchy install preinstalls | omarchy install preinstalls --all | omarchy install preinstalls Obsidian WhatsApp
# omarchy:requires-sudo=true

source "$OMARCHY_PATH/default/omarchy/preinstalls.sh"
preinstalls_run_install "$@"
