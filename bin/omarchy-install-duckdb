#!/bin/bash -e

curl -fsSL https://install.duckdb.org | bash

HYPR="$HOME/.config/hypr/bindings.conf"
mkdir -p "$(dirname "$HYPR")" && touch "$HYPR"
grep -q "SUPER.*SHIFT.*D" "$HYPR" || echo -e "\nbindd = SUPER SHIFT, D, DuckDB UI, exec, omarchy-launch-duckdb-ui" >>"$HYPR"
