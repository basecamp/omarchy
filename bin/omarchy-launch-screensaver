#!/bin/bash

# omarchy:summary=Launch the native Omarchy screensaver.
# omarchy:args=[force] [options]

screensaver=/usr/lib/omarchy-screensaver/omarchy-screensaver

if [[ -x $screensaver ]]; then
  exec "$screensaver" "$@"
else
  exit 1
fi
