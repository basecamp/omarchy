#!/bin/bash

# omarchy:summary=Restore follow_mouse after voxtype output
# omarchy:hidden=true

set -euo pipefail

STATE_FILE="${XDG_RUNTIME_DIR:-/tmp}/omarchy/voxtype-follow-mouse"

if [[ -f $STATE_FILE ]]; then
  hyprctl keyword input:follow_mouse "$(cat "$STATE_FILE")"
  rm -f "$STATE_FILE"
fi
