#!/bin/bash

STATE_FILE=~/.local/state/omarchy/toggles/screensaver-off

if [[ -f $STATE_FILE ]]; then
  rm -f $STATE_FILE
  notify-send -a omarchy "󱄄   Screensaver enabled"
else
  mkdir -p "$(dirname $STATE_FILE)"
  touch $STATE_FILE
  notify-send -a omarchy "󱄄   Screensaver disabled"
fi
