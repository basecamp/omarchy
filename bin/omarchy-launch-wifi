#!/bin/bash

if [[ "$TERMINAL" =~ ^(alacritty|kitty)$ ]]; then
    exec setsid uwsm app -- "$TERMINAL" --class=Impala -e impala "$@"
else
    exec setsid uwsm app -- "$TERMINAL" --title=Impala -e impala "$@"
fi
