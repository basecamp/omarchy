#!/bin/bash

if [[ "$TERMINAL" =~ ^(ghostty)$ ]]; then
    exec setsid uwsm app -- "$TERMINAL" --class=com.omarchy.impala -e impala "$@"
else
    exec setsid uwsm app -- "$TERMINAL" --class=Impala -e impala "$@"
fi
