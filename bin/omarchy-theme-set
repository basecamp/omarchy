#!/bin/bash

# omarchy-theme-set: Set a theme, specified by its name.
# Usage: omarchy-theme-set <theme-name>

if [[ -z "$1" && "$1" != "CNCLD" ]]; then
  echo "Usage: omarchy-theme-set <theme-name>" >&2
  exit 1
fi

THEMES_DIR="$HOME/.config/omarchy/themes/"
CURRENT_THEME_DIR="$HOME/.config/omarchy/current/theme"

THEME_NAME=$(echo "$1" | sed -E 's/<[^>]+>//g' | tr '[:upper:]' '[:lower:]' | tr ' ' '-')
THEME_PATH="$THEMES_DIR/$THEME_NAME"

# Check if the theme entered exists
if [[ ! -d "$THEME_PATH" ]]; then
  echo "Theme '$THEME_NAME' does not exist in $THEMES_DIR" >&2
  exit 2
fi

# Update theme symlinks
ln -nsf "$THEME_PATH" "$CURRENT_THEME_DIR"

# Change gnome modes
if [[ -f ~/.config/omarchy/current/theme/light.mode ]]; then
  gsettings set org.gnome.desktop.interface color-scheme "prefer-light"
  gsettings set org.gnome.desktop.interface gtk-theme "Adwaita"
else
  gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"
  gsettings set org.gnome.desktop.interface gtk-theme "Adwaita-dark"
fi

# Change gnome icon theme color
if [[ -f ~/.config/omarchy/current/theme/icons.theme ]]; then
  gsettings set org.gnome.desktop.interface icon-theme "$(<~/.config/omarchy/current/theme/icons.theme)"
else
  gsettings set org.gnome.desktop.interface icon-theme "Yaru-blue"
fi

# Set Zed theme if installed (check for both zed and zeditor commands)
if (command -v zed &>/dev/null || command -v zeditor &>/dev/null) && [[ -f "$CURRENT_THEME_DIR/zed.json" ]]; then
  mkdir -p ~/.config/zed/themes
  ln -nsf "$CURRENT_THEME_DIR/zed.json" ~/.config/zed/themes/omarchy-current.json

  # Update Zed's settings.json using sed (preserves comments and formatting)
  ZED_SETTINGS="$HOME/.config/zed/settings.json"
  if [[ -f "$ZED_SETTINGS" ]]; then
    if grep -q '"theme"' "$ZED_SETTINGS"; then
      sed -i 's/"theme"\s*:\s*"[^"]*"/"theme": "Omarchy Current"/g' "$ZED_SETTINGS"
    else
      # Add theme setting before the closing brace
      sed -i '$s/}/  "theme": "Omarchy Current"\n}/' "$ZED_SETTINGS"
    fi
  else
    # Create new settings.json with theme only if file doesn't exist
    echo '{
  "theme": "Omarchy Current"
}' > "$ZED_SETTINGS"
  fi

  # Touch the theme file to trigger Zed's file watcher for theme reload
  touch ~/.config/zed/themes/omarchy-current.json
fi

# Trigger alacritty config reload
touch "$HOME/.config/alacritty/alacritty.toml"

# Restart components to apply new theme
pkill -SIGUSR2 btop
omarchy-restart-waybar
omarchy-restart-swayosd
makoctl reload
hyprctl reload

# Set new background
omarchy-theme-bg-next
