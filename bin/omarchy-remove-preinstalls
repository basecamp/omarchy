#!/bin/bash

# omarchy:summary=Remove selected preinstalled Omarchy applications (web apps, TUIs, and selected packages).
# omarchy:args=[--all|name...]
# omarchy:examples=omarchy remove preinstalls | omarchy remove preinstalls --all | omarchy remove preinstalls LibreOffice

source "$OMARCHY_PATH/default/omarchy/preinstalls.sh"
preinstalls_run_remove "$@"
