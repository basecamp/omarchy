#!/bin/bash

KEYBOARD_THEME=~/.config/omarchy/current/theme/keyboard.rgb

if omarchy-cmd-present nuphyctl && [[ -f $KEYBOARD_THEME ]]; then
  nuphyctl rgb set \
    --hex "$(sed 's/^#//' "$KEYBOARD_THEME")" \
    --vid 0x19f5 --pid 0x1028 \
    >/dev/null 2>&1
fi
