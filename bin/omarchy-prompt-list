#!/bin/bash

find ~/.local/share/omarchy/config/starship -mindepth 1 -maxdepth 1 \( -type f -o -type l \) | sort | while read -r path; do
  echo "$(basename "$path" | sed -E 's/\.[^.]+$//' | sed -E 's/(^|-)([a-z])/\1\u\2/g; s/-/ /g')"
done
