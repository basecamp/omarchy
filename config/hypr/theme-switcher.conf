# Theme switching configuration

# Interval between theme switches
# Options: min, hour, day, week, month
# Default: hour
interval = min

# Mode for theme switching
# Options: off, next, bg-next, random
# Default: off
mode = off

